---
# Role default variables
required_storage_gb: 500
eth_rpc_url: "{{ lookup('env', 'ETH_RPC_URL') | default('', true) }}"
starknet_network: "{{ lookup('env', 'STARKNET_NETWORK') | default('mainnet', true) }}"